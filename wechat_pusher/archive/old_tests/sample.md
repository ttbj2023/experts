# Markdown 转 Word 兼容 HTML 示例

## 文档说明

这是一个示例 Markdown 文件，展示了所有支持的功能特性。转换后的 HTML 文件可以用 Microsoft Word 打开，并另存为 DOCX 格式。

---

## 1. 基础文本格式

### 1.1 文本样式

这是 **粗体文本**，这是 *斜体文本*，这是 ***粗斜体文本***。

这是 ~~删除线文本~~（注：Markdown标准删除线）。

这是 `行内代码` 示例。

上标示例：E=mc^2^，下标示例：H~2~O（注：扩展语法）。

### 1.2 引用块

> 这是一段引用文本。
> 可以有多行。
>
> —— 引用来源

---

## 2. 列表

### 2.1 无序列表

- 第一项：这是一段很长的文字内容，用来测试在移动端设备上显示时的换行效果。当屏幕宽度有限时，这段文字会自动换行到下一行，我们需要确保换行后的文字排版整齐美观，不会出现奇怪的对齐问题。
- 第二项：另一段长文字内容，包含了一些技术术语和详细说明，比如"在微信公众号移动端显示时，文字的行高和字间距会自动调整，以适应不同设备的显示效果，提升用户的阅读体验"。
- 第三项（嵌套测试）
  - 嵌套项目 A：这是嵌套列表的第一项，包含了一段较长的说明文字，用来测试嵌套列表在移动端的显示效果是否正常。
  - 嵌套项目 B：第二层嵌套列表项，这里还有更多的文字内容来验证缩进和间距设置是否合理。
- 第四项：继续添加更多内容，这是一个包含大量文字的列表项，用来测试当文字内容非常长时，列表的显示效果如何，包括换行、对齐、间距等各个方面。

### 2.2 有序列表

1. **第一项**：这是有序列表的第一项，包含了一段详细的技术说明，讨论了如何在移动端优化列表的显示效果，包括调整行高、字间距、边距等多个方面的内容，确保用户在手机上阅读时有良好的体验。
2. **第二项**（包含嵌套）：
   1. 子项目 2.1：这是第一层的嵌套列表项，包含了更多的文字内容，用来测试多层嵌套列表在移动端的显示效果。
   2. 子项目 2.2：另一个嵌套列表项，这里的文字内容也比较长，需要确保在移动端换行后仍然保持良好的可读性。
3. **第三项**：最后一个有序列表项，这里我们加入了一些总结性的文字，说明列表在各种设备上的显示效果都需要经过仔细的测试和优化，以确保用户体验的一致性。

### 2.3 教辅题目示例

1. 下列说法正确的是（）
   A. 选项内容A：这是一段很长的选项说明，用来测试选择题在移动端的显示效果，当选项文字很长时，需要有合理的换行和对齐方式。
   B. 选项内容B：这是第二个选项，同样包含了一段较长的文字描述，用来确保所有选项都能正确显示。
   C. 选项内容C：继续添加更多内容，测试各种情况下的显示效果。
   D. 选项内容D：最后一个选项，也包含了详细的文字说明。

2. 计算题（长题目测试）
   (1) 第一步：计算平均数。首先需要将所有的数据相加，然后除以数据的个数，得到平均数。这是一个详细的步骤说明，用来测试长段落文字在列表中的显示效果。
   (2) 第二步：计算方差。根据平均数计算每个数据点与平均数的差值，然后平方这些差值，最后求平均得到方差。这里也有大量的文字说明，用来验证移动端的显示效果。

---

## 3. 数学公式

### 3.1 行内公式

方差计算公式为 $s^2 = \frac{1}{n}\sum_{i=1}^n (x_i-\overline{x})^2$。

欧拉公式：$e^{i\pi} + 1 = 0$。

### 3.2 块级公式

二次方程求根公式：

$$x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}$$

定积分示例：

$$\int_{a}^{b} f(x) dx = F(b) - F(a)$$

矩阵示例：

$$\begin{pmatrix}
a & b \\
c & d
\end{pmatrix}$$

---

## 4. 表格

### 4.1 简单表格

| 姓名 | 年龄 | 职业 |
|------|------|------|
| 张三 | 25 | 工程师 |
| 李四 | 30 | 设计师 |
| 王五 | 28 | 产品经理 |

### 4.2 教辅数据表

| 组别 | 平均数 | 方差 | 极差 |
|------|--------|------|------|
| A组 | 4℃ | 127.25 | 8 |
| B组 | 20℃ | 53.1875 | 5 |
| C组 | 15℃ | 89.5 | 7 |

---

## 5. 代码块

### 5.1 Python 代码

```python
def calculate_variance(data):
    """计算方差"""
    n = len(data)
    mean = sum(data) / n
    variance = sum((x - mean) ** 2 for x in data) / n
    return variance

# 示例使用
data = [4, 8, 6, 5, 3]
result = calculate_variance(data)
print(f"方差: {result}")
```

### 5.2 JavaScript 代码

```javascript
function calculateSum(arr) {
    return arr.reduce((sum, num) => sum + num, 0);
}

// 示例
const numbers = [1, 2, 3, 4, 5];
const total = calculateSum(numbers);
console.log(`总和: ${total}`);
```

---

## 6. 多栏布局（示例）

::: two-column

**左栏内容**

这里是第一栏的文字内容。可以包含题目、说明等。例如：

1. 题目一
2. 题目二
3. 题目三

**右栏内容**

这里是第二栏的文字内容。可以包含答案、解析等。例如：

- 答案一：解析内容
- 答案二：解析内容
- 答案三：解析内容

:::

---

## 7. 水平分隔线

上面是章节内容。

---

下面是新章节。

---

## 8. 使用说明

### 转换步骤

1. 使用转换命令：
   ```bash
   ./convert-md-to-html.py sample.md -o output/
   ```

2. 用 Microsoft Word 打开生成的 HTML 文件：
   - 输出路径：`output/sample.html`

3. 检查排版、公式、图片是否正常

4. 文件 → 另存为 → 选择 `.docx` 格式

5. 完成！DOCX 文件可直接编辑

### 支持的特性

- ✅ HTML 4.01 + CSS 2.1 完全兼容
- ✅ Base64 内嵌图片（无裂图）
- ✅ MathJax 公式（可编辑）
- ✅ 多栏布局
- ✅ 复杂表格
- ✅ 多层列表
- ✅ 代码块

---

## 附录：测试数据

### 统计数据

| 指标 | 数值 |
|------|------|
| 页数 | 1 |
| 字数 | 约 1500 |
| 公式数 | 5 |
| 表格数 | 2 |
| 代码块数 | 2 |

### 联系方式

如有问题，请参考项目文档。

---

**文档结束**
