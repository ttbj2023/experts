# Qwen3 VL 8B 优化配置
# 针对 16GB 显存 (RTX 4070 TiS) 的文档处理场景优化

# 基础模型
FROM qwen3-vl:8b

# ============================================
# 上下文窗口配置（关键！）
# ============================================
# Token 计算：视觉Token = (宽×高) / (32×32) + 2
# 1080p图片 ≈ 2100 视觉Token
#
# 标准方案（12K）- 推荐！
# - 视觉输入: ~2100 tokens (1080p)
# - 提示词: ~500 tokens
# - 思考过程: ~4000 tokens
# - 输出文本: ~2000 tokens
# - 安全余量: ~3400 tokens
# - 总计: ~12K tokens
# - 显存占用: ~9GB
# - 速度: 快速（~40s/页）
PARAMETER num_ctx 12288

# 平衡方案（16K）- 处理复杂页面
# - 适合多图页面或长输出
# - 显存占用: ~11GB
# - 速度: 中等（~50s/页）
# PARAMETER num_ctx 16384

# 激进方案（24K）- 超长文档或2K+高分辨率
# PARAMETER num_ctx 24576
# 风险：可能接近显存上限，需测试

# ============================================
# 温度参数（需根据场景测试）
# ============================================
# 推荐温度值范围：
# - 0.0-0.1: 确定性输出（OCR、结构化数据）
# - 0.2-0.3: 平衡模式（描述性文本）
# - 0.4-0.5: 创造性输出（一般不需要）

# 当前 GLM 最优值: 0.05
# Qwen3 VL 建议起始值: 0.1（比 GLM 稍高）

PARAMETER temperature 0.1
PARAMETER top_p 0.9
PARAMETER top_k 40

# ============================================
# 显存优化
# ============================================
# 批处理大小（影响速度和质量）
PARAMETER num_batch 512

# GPU 层数（自动检测）
# PARAMETER num_gpu 99

# ============================================
# 停止词配置（避免冗余输出）
# ============================================
PARAMETER stop "<|im_end|>"
PARAMETER stop "<|end|>"
PARAMETER stop "<|eot_id|>"

# ============================================
# 系统提示词模板
# ============================================
TEMPLATE """
{{- if .System }}
<|im_start|>system
{{ .System }}<|im_end|>
{{- end }}

{{- range .Messages }}
<|im_start|>{{ .Role }}
{{ .Content }}<|im_end|>
{{- end }}

<|im_start|>assistant
"""

# ============================================
# 使用说明
# ============================================
# 1. 创建自定义模型：
#    ollama create qwen3-vl-8b-doc -f config/ollama_modelfile_qwen3vl_8b.md
#
# 2. 使用自定义模型：
#    ollama run qwen3-vl-8b-doc
#
# 3. 在代码中更新模型名称：
#    vision_model: "qwen3-vl-8b-doc"
#
# ============================================
# 参数测试计划
# ============================================
# 场景1: OCR识别（高精度要求）
#   temperature: 0.0, 0.05, 0.1
#   num_ctx: 8192（单页足够）
#
# 场景2: 图片描述（中等创造性）
#   temperature: 0.1, 0.2, 0.3
#   num_ctx: 4096
#
# 场景3: 语义匹配（低创造性）
#   temperature: 0.0, 0.05
#   num_ctx: 32768（需要全局上下文）
#
# ============================================
# 性能参考（4070TiS 16GB）
# ============================================
# 上下文长度    视觉Token   显存占用    速度      适用场景
# 8K           720p        ~8GB       快速      简单文档
# 12K          1080p       ~9GB       快速      标准文档 ⭐ 推荐
# 16K          1080p       ~11GB      中等      复杂页面
# 24K          2K          ~14GB      较慢      高分辨率
# 32K          4K          ~16GB+     很慢      超高清（可能OOM）
#
# Token 计算公式：
#   视觉Token = (宽×高) / (32×32) + 2
#   1080p = 1920×1080 ≈ 2100 tokens
#   2K = 2560×1440 ≈ 3600 tokens
#   4K = 3840×2160 ≈ 8100 tokens
#
# 完整请求 = 视觉Token + 提示词(500) + 思考(4K) + 输出(2K)
